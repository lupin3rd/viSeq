story_id: e01s03
title: "OSC + state sync + media grid + thumbnails + monitor players on 2.x"
status: failing
bcps: 3
tasks:
  - id: 1
    description: "Re-verify incoming_osc_handler caps (MED-6) and update_vimix_sources_ui payload validation / defensive sorting (MED-4) on 2.x via the harness"
    verify: "python3 tests/test_fixes.py 2>&1 | grep -c 'MED-4\\|MED-6' | grep -qE '^(8|9|1[0-2])$'"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["migration", "security"]
  - id: 2
    description: "L-1 stale-state pruning: drop thumbnails_data/request_timestamps entries (and orphaned tex_<id> textures) for sources no longer in the vimix state"
    verify: "python3 -c \"import re; src=open('viseq.py').read(); assert 'thumbnails_data' in src.split('def update_vimix_sources_ui')[1].split('def thumbnail_decoder_worker')[0], 'prune logic missing from state path'; print('OK')\""
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["migration", "memory"]
  - id: 3
    description: "Add an L-1 regression check to the harness (source removed -> texture entry pruned)"
    verify: "python3 tests/test_fixes.py 2>&1 | grep -c 'L-1' | grep -q 1"
    risk: P2
    status: failing
    allure:
      severity: normal
      categories: ["regression"]
  - id: 4
    description: "Re-verify thumbnail pipeline (decoder pixel cap, texture upload, tile rebuild) and monitor players on 2.x via the harness"
    verify: "python3 tests/test_fixes.py"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["migration", "media"]
