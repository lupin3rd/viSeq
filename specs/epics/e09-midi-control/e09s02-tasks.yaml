story_id: e09s02
title: "MIDI Learn flow + MIDI menu (Enable, Learn, Mappings window with Delete, Save)"
status: failing
bcps: 3
tasks:
- id: 1
  description: "Learn-capture plumbing: midi_learn_mode flag + pending action; learnable() wrapper (when learn mode is on, a wrapped widget callback records its action instead of executing; otherwise delegates); worker-side capture of the next incoming message -> create binding (device/channel/type/number from the message)"
  verify: ".venv/bin/python -m pytest tests/ -q -k 'midi_learn'"
  risk: P1
  status: failing
  allure:
    severity: high
    categories: ["midi", "ui"]
- id: 2
  description: "Learnable widgets on the sequencer window: step-cell checkbox -> seq_toggle(row,col), PLAY -> transport_play, RESYNC, TAP, nudge <>, beat-source checkboxes, ASSIGN CLIP -> track_assign(row)"
  verify: ".venv/bin/python -m pytest tests/ -q -k 'learnable'"
  risk: P1
  status: failing
  allure:
    severity: high
    categories: ["midi", "sequencer"]
- id: 3
  description: "MIDI menubar menu + Mappings window: Enable checkbox, Learn item (toggle + status hint), Mappings window listing all bindings with per-row Delete, Save button persisting via save_config; Italian-free English labels"
  verify: ".venv/bin/python -m pytest tests/ -q -k 'midi_menu or midi_mappings'"
  risk: P1
  status: failing
  allure:
    severity: high
    categories: ["midi", "windowing"]
