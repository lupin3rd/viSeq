# specs/epics/e03-window-manager-restructure/epic.yaml — capsule manifest (slice-tasks)
id: e03
slug: window-manager-restructure
title: "Menubar shell: Settings window, Show menu + Logs (newest-first), borderless clip slot"
status: in_progress
delta: ENHANCED   # app-shell restructure; no behavior/contract change for OSC or sequencer
context: >-
  The app opens every utility window at boot (viOSC config, OSC Logs, Audio analyzer,
  Vimix Media). The user wants a menubar-driven shell: a "Settings" entry opens a hidden
  general-settings window (today only the OSC section, more fields later), a "Show" menu
  opens the hidden Logs window (rendered newest-first), and the sequencer's clip slot drops
  its bordered frame and centers the bare assign button. The viOSC window content moves
  verbatim under an "OSC" section; autostart_osc keeps working with the window hidden.
stories:
  - id: e03s01
    title: "Settings window (hidden, opened from menubar) hosting the OSC config section"
    bcps: 2
    delta: ENHANCED
  - id: e03s02
    title: "Show menu + Logs window (hidden by default) with newest-first rendering"
    bcps: 2
    delta: ENHANCED
  - id: e03s03
    title: "Clip slot: centered bare assign button, no table frame"
    bcps: 2
    delta: ENHANCED
references:
  - "specs/product/SCOPE_LATEST.yaml"
  - "specs/planning-context.yaml"
