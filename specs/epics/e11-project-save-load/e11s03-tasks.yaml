# specs/epics/e11-project-save-load/e11s03-tasks.yaml
story_id: e11s03
title: "viSeq menu — Open/Last/Save/Exit + native file dialogs + Last-project submenu rebuild"
status: passing
bcps: 4
tasks:
  - id: 1
    description: "RED: menu-structure tests — viSeq first menubar menu, contains Open/Save/Exit + menu_last_project submenu, existing menus keep order"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'viseq_menu or last_project_menu'"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viseq"]
  - id: 2
    description: "GREEN: viewport_menu_bar restructure + open/save dialog creation with stable tags"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'viseq_menu or last_project_menu'"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viseq"]
  - id: 3
    description: "RED: flow tests — open_project_file applies+remembers+rebuilds; bad file -> False no crash; save_project_file round-trips"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'project_flow'"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viseq"]
  - id: 4
    description: "GREEN: open_project_file / save_project_file / rebuild_last_project_menu (empty -> disabled item)"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'project_flow'"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viseq"]
  - id: 5
    description: "RED: dialog+exit tests — exit_app records stop_dearpygui; save callback appends .viseq; open callback routes to open_project_file"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'project_dialog'"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 6
    description: "GREEN: dialog callbacks + exit_app"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'project_dialog'"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 7
    description: "Full-suite verification"
    verify: ".venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check . && .venv/bin/mypy"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
