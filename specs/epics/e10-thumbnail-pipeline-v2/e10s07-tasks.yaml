# specs/epics/e10-thumbnail-pipeline-v2/e10s07-tasks.yaml
story_id: e10s07
title: "Beat sources: only Band 1 selectable in the sequencer + deadlock-free source switching (BUG-2026-08-27T213000)"
status: passing
bcps: 3
tasks:
  - id: 1
    description: "RED: deadlock regression — sequencer_tick's band/MIDI wait must be polled with a timeout (structural test, same pattern as test_sequencer_waits_once_per_step)"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'wait_is_polled or waits_once'"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viseq", "Bugfix"]
  - id: 2
    description: "GREEN: replace the unbounded sync_event_beat.wait() with a bounded poll (advance only on a real event, otherwise re-loop so mode/stop are re-evaluated)"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'wait_is_polled or waits_once'"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viseq", "Bugfix"]
  - id: 3
    description: "RED: update beat-source tests to the 4-entry list (no band2/band3 checkboxes/LEDs, no event-driven membership); add a test that a band-2/3 rise never fires the sequencer beat"
    verify: ".venv/bin/python -m pytest tests/ -q -k beat"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 4
    description: "GREEN: remove Band 2/3 from BEAT_SOURCE_LABELS / BEAT_LED_TAGS / beat_is_event_driven and the sequencer checkbox loops; band 1 is the only beat-capable band; guard flash_led against unknown beat_source (stale MIDI binding)"
    verify: ".venv/bin/python -m pytest tests/ -q -k beat"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 5
    description: "Full-suite verification"
    verify: ".venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check . && .venv/bin/mypy"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
