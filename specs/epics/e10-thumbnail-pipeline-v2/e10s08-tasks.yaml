# specs/epics/e10-thumbnail-pipeline-v2/e10s08-tasks.yaml
story_id: e10s08
title: "Sequencer: stale BPM never drives the clock + compact single-row beat sources"
status: passing
bcps: 4
tasks:
  - id: 1
    description: "RED: _timed_bpm_live() tests — manual always live; analysis live only with tracking on and a detection within the 2 s stale window; structural test that the analyzer stamps bpm_last_detected"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'timed_bpm or essentia_loop_marks'"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 2
    description: "GREEN: bpm_last_detected + _timed_bpm_live() + gating in sequencer_tick and visual_metronome_loop"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'timed_bpm or essentia_loop_marks'"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq", "Bugfix"]
  - id: 3
    description: "RED: UI tests — abbreviated labels (BPM Det / Band 1 / MIDI / Manual); single-row layout (alignment-spacer test removed)"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'beat_source_labels or beat_source_ui_wired'"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 4
    description: "GREEN: single-row layout + label abbreviations + manual_bpm_text show toggle in midi_action_beat_source"
    verify: ".venv/bin/python -m pytest tests/ -q -k 'beat_source_labels or beat_source_ui_wired'"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 5
    description: "Full-suite verification (HIGH-2 fade tests run in Manual mode — a live timed tempo)"
    verify: ".venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check . && .venv/bin/mypy"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
