# specs/epics/e10-thumbnail-pipeline-v2/e10s05-tasks.yaml
story_id: e10s05
title: "Thumb cycling in the sequencer clip slots and monitor players (stable consumer tags, shared visibility gate)"
status: passing
bcps: 4
tasks:
  - id: 1
    description: "RED: consumer-tag + multi-consumer cycle tests — slot image button tagged seq_thumb_<row>, monitor image tagged mon_thumb_<id>, tick_thumb_cycle switches grid tile + slot + monitor of one source to the same frame index on the 750 ms cadence"
    verify: ".venv/bin/python -m pytest tests/ -q -k cycle"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 2
    description: "GREEN: tag the slot/monitor images; extend tick_thumb_cycle to configure every consumer of the source with the cycled index (does_item_exist guards, same style as the grid tile)"
    verify: ".venv/bin/python -m pytest tests/ -q -k cycle"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 3
    description: "RED: gate test — cycling advances when the Mediagrid is hidden but the sequencer (or a monitor) window is visible; pauses when all consumer windows are hidden"
    verify: ".venv/bin/python -m pytest tests/ -q -k cycle"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq"]
  - id: 4
    description: "GREEN: _thumb_cycle_active() predicate over the Mediagrid/sequencer/monitor window sets; full-suite verification incl. frame_sleep regression (cycling must not force full rate)"
    verify: ".venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check . && .venv/bin/mypy"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories: ["unit", "viseq", "performance"]
