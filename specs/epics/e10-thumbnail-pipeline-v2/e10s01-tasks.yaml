# specs/epics/e10-thumbnail-pipeline-v2/e10s01-tasks.yaml
story_id: e10s01
title: "viOSC generation hardening — log failures, keep good cache, regenerate on demand (implements BUG-2026-08-26T230838)"
status: passing
bcps: 3
tasks:
  - id: 1
    description: "viosc repo: create .venv (python3 -m venv .venv), install -r requirements.txt + pytest; add tests/test_thumbnails.py importing viosc (python-osc only; subprocess/os.path monkeypatched)"
    verify: "cd ../viosc && test -f .venv/bin/python && .venv/bin/python -m pytest --version"
    risk: P3
    status: passing
    allure:
      severity: minor
      categories: ["unit", "viosc"]
  - id: 2
    description: "RED: tests asserting the generation worker logs a reason-specific error for (a) missing file, (b) ffmpeg non-zero exit, (c) empty output"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viosc", "Bugfix"]
  - id: 3
    description: "GREEN: add the three reason-specific error branches to the generation worker's failure paths; keep the success print"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viosc", "Bugfix"]
  - id: 4
    description: "RED: test asserting a FAILED regeneration leaves the previously cached thumbnail untouched (cache replaced only when extraction yields >=1 frame)"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viosc", "Bugfix"]
  - id: 5
    description: "GREEN: only assign the new result to the thumbnail cache when non-empty; keep the old cache on failure"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viosc", "Bugfix"]
  - id: 6
    description: "RED: test asserting an on-demand thumb request with empty cache + valid URI regenerates (bounded by the concurrency semaphore) and replies with the fresh frame"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["unit", "viosc", "Bugfix"]
  - id: 7
    description: "GREEN: reply path regenerates then sends when cache empty + URI valid; silent no-op only for unloadable sources (no URI / file missing after retry)"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q && cd ../viseq && .venv/bin/python -m pytest tests/ -q"
    risk: P1
    status: passing
    allure:
      severity: high
      categories: ["integration", "viosc", "viseq", "Bugfix"]
