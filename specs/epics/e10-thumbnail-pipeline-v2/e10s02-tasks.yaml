# specs/epics/e10-thumbnail-pipeline-v2/e10s02-tasks.yaml
story_id: e10s02
title: "viOSC generates up to 3 thumbs per media at distinct jittered times"
status: failing
bcps: 2
tasks:
  - id: 1
    description: "RED: tests asserting (a) count=3 requests 3 distinct timestamps and returns 3 frames, (b) duration<=0 yields exactly 1 frame, (c) very short clips yield min(distinct frames, 3)"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P2
    status: failing
    allure:
      severity: normal
      categories: ["unit", "viosc"]
  - id: 2
    description: "GREEN: implement distinct-time frame picking (~15/50/85 % with jitter, collision-safe) — 3 subprocess calls OR one multi-frame ffmpeg invocation, whichever e10s04's spike shows is cheaper; per-frame pixel/blob caps stay"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q -k thumb"
    risk: P2
    status: failing
    allure:
      severity: normal
      categories: ["unit", "viosc"]
  - id: 3
    description: "Full-suite verification both repos (generation worker stores the list; reply path with 'all' sends every index, contract unchanged)"
    verify: "cd ../viosc && .venv/bin/python -m pytest tests/ -q && cd ../viseq && .venv/bin/python -m pytest tests/ -q"
    risk: P2
    status: failing
    allure:
      severity: normal
      categories: ["integration", "viosc", "viseq"]
