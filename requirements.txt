# viseq runtime dependencies — Python 3.13 target (epic e01)
#
# DearPyGui 2.x keeps the 1.x API surface viseq imports (dearpygui.dearpygui) — see
# specs/archive/spikes/SPIKE-dpg2x-api.md. Wheel availability (verified via PyPI):
#   dearpygui 2.3.1            -> cp313 wheel
#   essentia 2.1b6.dev1389     -> cp313 wheel (newer dev1438 is cp314-only)
#
# Install with a Python 3.13 venv:
#   python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
dearpygui==2.3.1
essentia==2.1b6.dev1389
numpy>=2,<3
python-osc==1.10.2
sounddevice==0.5.6
Pillow>=11,<13
