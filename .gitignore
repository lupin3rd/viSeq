# Python
__pycache__/
*.pyc
*.pyo

# Virtualenv
.venv/
venv/

# OS
.DS_Store

# Local spike/temp artifacts
/tmp/

# AppImage build artifacts (e21s02)
dist/
.appimage-cache/
# Local AppImage build tooling — kept local only (release repo stays minimal)
scripts/

# Gate tooling caches (G6)
.mypy_cache/
.pytest_cache/
.ruff_cache/
viseq_config.json

# User project files (saved via viSeq > Save project)
/projects/

# Internal agent/spec/dev files — never published (user decision, kept local only)
AGENTS.md
CLAUDE.md
CONVENTIONS.md
specs/
tests/
pyproject.toml
.pre-commit-config.yaml
requirements-dev.txt
